{
  "name": "video-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fully-automated polished promo videos from our websites (deterministic Playwright capture + ffmpeg post).",
  "bin": {
    "promo": "build.mjs"
  },
  "scripts": {
    "build": "node build.mjs",
    "capture": "node build.mjs --capture-only",
    "assemble": "node build.mjs --assemble-only"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
